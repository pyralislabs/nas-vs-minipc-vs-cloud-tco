/**
 * Size limits for input validation.
 *
 * These are the single source of truth for size caps.
 * Changing these requires a Changeset.
 */
export const CLI_INPUT_MAX_BYTES = 1_048_576; // 1 MiB
export const WIDGET_IMPORT_MAX_BYTES = 262_144; // 256 KiB
